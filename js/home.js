import {supabase} from './supabase-config.js'; const box=document.getElementById('videos'),status=document.getElementById('status'); const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(q=''){status.textContent='Loading...';let x=supabase.from('videos').select('id,title,description,views,created_at,profiles(username)').eq('visibility','public').order('created_at',{ascending:false});if(q)x=x.ilike('title',`%${q}%`);const {data,error}=await x;if(error){status.textContent=error.message;return}status.textContent=data.length?'':'No videos found.';box.innerHTML=data.map(v=>`<article><h3><a href="watch.html?id=${v.id}">${esc(v.title)}</a></h3><p>Uploaded by: ${esc(v.profiles?.username||'Unknown')}</p><p>Views: ${v.views}</p></article><hr>`).join('')}
document.getElementById('searchForm').addEventListener('submit',e=>{e.preventDefault();load(document.getElementById('search').value.trim())});load();
