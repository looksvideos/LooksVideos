# Plain HTML Supabase Video Site

Run `supabase/schema.sql` in Supabase SQL Editor. The frontend is plain HTML with JavaScript modules and no CSS. Configure Supabase Auth redirect URLs for your deployed site. The app enforces a 100 MB video selection/database limit. Supabase plan storage/upload limits still apply.
